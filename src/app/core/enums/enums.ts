export enum FormMode {
  VIEW,
  NEW,
  EDIT
}

export enum ConfirmDialogModal {
  DECLINE,
  APPROVE
}

export enum Folder {
  SPEECHES,
  FAVORITES
}


export enum SearchCategory {
  CONTENT = 'content',
  AUTHOR = "author",
  KEYWORD = "keywords",
  TITLE = "title"
}